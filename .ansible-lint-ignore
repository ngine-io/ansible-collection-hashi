# hashi_common__* is deliberate: these variables are read by the nomad and
# consul roles, not just by the common role, so they are namespaced for the
# collection rather than for the role that declares them.
roles/common/defaults/main.yml var-naming[no-role-prefix]
.claude/*
